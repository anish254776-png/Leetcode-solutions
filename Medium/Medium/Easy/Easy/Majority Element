class Solution {
public:
    int majorityElement(vector<int>& nums) {
        int cnt=0,cand=0;
        for(int x:nums){
            if(cnt==0){cand=x; cnt=1;}
            else cnt += (x==cand?1:-1);
        }
        return cand;
    }
};
