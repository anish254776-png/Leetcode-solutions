class Solution {
public:
    int missingNumber(vector<int>& nums) {
        int n = nums.size();
        int xr = n;
        for (int i = 0; i < n; i++) xr ^= i ^ nums[i];
        return xr;
    }
};
